# 伏线 v2.1 · Cloudflare-ready 改动说明

本版本在 v2“大纲 + 正文”完整工作台基础上完成了 Cloudflare 原生部署整理。

## 保留的产品能力

- 多本小说切换
- 每本书独立“大纲 / 正文”
- 软木板章节、人物、伏笔、脑洞、便签与关系连线
- 正文逐章云端存储、自动保存、本地未同步草稿
- 正文章节通过稳定的 `outlineCardId` 关联大纲章节
- 正文右侧同步显示本章大纲与人物标签
- 大纲快照、导出、revision 冲突保护

## Cloudflare 部署改造

1. 删除 OpenAI Sites 专用 `.openai/hosting.json`、Sites Vite plugin 和执行环境包装脚本。
2. `vite.config.ts` 改为标准 `vinext() + cloudflare()` 配置。
3. 新增 `wrangler.jsonc`，Worker 名为 `fuxian-novel-workbench`，D1 binding 固定为 `DB`。
4. 新增 Cloudflare Access JWT 验证，不再读取 OpenAI 专用登录 header。
5. JWT 验证包含 RS256 签名、issuer、audience、exp/nbf 与身份字段检查。
6. 新增 `/api/health`，用于上线后确认 Worker、D1 与 Access 配置状态。
7. 新增 `CLOUDFLARE_DEPLOY.md` 与 `pnpm cf:preflight` 部署前检查。
8. 本地 `NODE_ENV=development` 使用固定开发身份，生产环境不存在认证旁路。

## 正式部署前仍需填写的账号专属值

源码不能替代你的 Cloudflare 账号生成以下值：

- D1 Database ID
- Cloudflare Access Team Domain
- Cloudflare Access Application Audience (AUD) Tag

其中 D1 Database ID 写入 `wrangler.jsonc`；Team Domain 与 AUD 推荐在 Worker 的 Variables & Secrets 中配置。
